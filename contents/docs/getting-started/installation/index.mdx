---
title: Installation
description: Installation guide for react-components.
---

Setting up **react-components** is simple and flexible. You can install it via npm/yarn or clone the repository for local development. Ensure you have [Node.js](https://nodejs.org/) version 18 or higher for optimal performance.

## Install via npm/yarn/pnpm

To install **react-components** in your React or Next.js project, use one of the following commands:

```plaintext
npm install @elizabthpazp/react-components
```

or

```plaintext
yarn add @elizabthpazp/react-components
```

or

```plaintext
pnpm add @elizabthpazp/react-components
```

## Clone Repository

If you prefer working with the source code, you can clone the GitHub repository and set up the project manually.

<Stepper>
  <StepperItem title="Step 1: Clone the Repository">
    Clone the **react-components** repository from GitHub:
    
    ```plaintext
    git clone https://github.com/elizabthpazp/react-components.git
    ```
  </StepperItem>

  <StepperItem title="Step 2: Navigate into the Project">
    After cloning, move into the project directory:
    
    ```plaintext
    cd react-components
    ```
  </StepperItem>

  <StepperItem title="Step 3: Install Dependencies">
    Install all necessary dependencies with npm:
    
    ```plaintext
    npm install
    ```
  </StepperItem>

  <StepperItem title="Step 4: Start the Development Server">
    Launch the development server to test the library locally:
    
    ```plaintext
    npm run dev
    ```
  </StepperItem>
</Stepper>

With this setup, you'll have **react-components** ready to enhance your UI development efficiently!