---
title: Quick Start Guide
description: Get started with react-components quickly and efficiently.
---

Follow these simple instructions to set up **react-components** and integrate reusable UI components into your project. 🚀

## Installation

1. **Install the Library**
   
   Use npm or yarn or pnpm to install **react-components** in your project:

   ```sh
   npm install @elizabthpazp/react-components
   ```
   
   or
   
   ```sh
   yarn add @elizabthpazp/react-components
   ```

   or
   
   ```sh
   pnpm add @elizabthpazp/react-components
   ```

2. **Import and Use Components**

   - Import styles globally in your project:

     ```javascript
     import "@elizabthpazp/react-components/dist/globals.css";
     ```

   - Import and use individual components:

     ```javascript
     import { ButtonCard } from "@elizabthpazp/react-components";

     function App() {
       const [userData, setUserData] = useState(null);

       const handleLogin = (email, password, rememberMe) => {
        setUserData({ email, password, rememberMe }); 
       };

       return <ButtonCard title={'Login'} darkMode={true} onLogin={handleLogin}></ButtonCard>;
     }

     export default App;
     ```

## Development & Contribution

If you want to modify or contribute to the library, follow these steps:

1. **Clone the Repository**
   ```sh
   git clone https://github.com/elizabthpazp/react-components.git
   ```
2. **Navigate into the Project**
   ```sh
   cd react-components
   ```
3. **Install Dependencies**
   ```sh
   npm install
   ```
4. **Run the Development Server**
   ```sh
   npm run dev
   ```

## Explore Further

This guide helps you quickly integrate **react-components** into your project. For more details, explore the documentation or check the repository on GitHub. 🚀